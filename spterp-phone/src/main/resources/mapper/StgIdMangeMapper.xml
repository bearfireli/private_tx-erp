<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hntxrj.mapper.StgIdMangeMapper">
   <!-- 砼标号管理-->
    <select id="getStgidManage" resultType="com.hntxrj.vo.StgidManageVO">
       select StgId,grade,pumpPrice,notPumpPrice,towerCranePrice,PriceDifference
       from SM_GradePriceInfo
        where compid =#{compid}
          and RecStatus =1
        <if test="stgId !=null and stgId !='' ">
            and stgId like '%'+#{stgId}+'%'
        </if>
        <if test="grade !=null and grade !='' ">
            and grade like '%'+#{grade}+'%'
        </if>
    </select>

</mapper>